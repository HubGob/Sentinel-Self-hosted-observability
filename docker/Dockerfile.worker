FROM python:3.13-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml ./
RUN uv sync --no-dev --no-install-project

COPY sentinel/ sentinel/
COPY apps/ apps/
RUN uv sync --no-dev

CMD ["uv", "run", "python", "-m", "apps.worker"]
